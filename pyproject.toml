[project]
name = "freelance-analyser"
version = "0.1.0"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
include = ["src*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
cache_dir = "/tmp/.pytest_cache"
